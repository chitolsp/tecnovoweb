<div id = "page-header">
<h1>Listado de productos</h1>

	
    <% @productos.each do |producto| %>
    <hr />
    <div class="productos">
		<h3><%= producto.articulos %></h3>
		<p>Cantidad: <%= producto.cantidad %></p>
		<p>Descripcion: <%= producto.descripcion %></p>
		<p><%= link_to 'Mostrar', producto %> 
			<span="admin">
				- <%= link_to 'Editar', edit_producto_path(producto) %> - <%= link_to 'Eliminar', producto, method: :delete, data: { confirm: '¿Estas seguro?' } %></p>
			</span>
	 </div>

    <% end %>


<%= link_to 'Nuevo producto', new_producto_path, class:"btn btn-success" %>
</div>